{"id": "pyyaml-load-untrusted", "package": "pyyaml", "advisory": "Arbitrary code execution via yaml.load() (or FullLoader) on untrusted input. Only exploitable when untrusted data is passed to load(); yaml.safe_load() is unaffected.", "code_usage": "The web app calls yaml.load(request.body) to parse user-submitted YAML payloads.", "gold_label": "reachable", "note": "Untrusted input reaches the unsafe loader directly."}
{"id": "pyyaml-safeload", "package": "pyyaml", "advisory": "Arbitrary code execution via yaml.load() on untrusted input; yaml.safe_load() is unaffected.", "code_usage": "The app only ever calls yaml.safe_load(), and only on its own bundled config files.", "gold_label": "not-reachable", "note": "Condition (yaml.load on untrusted data) is not met."}
{"id": "pyyaml-loader-ambiguous", "package": "pyyaml", "advisory": "Arbitrary code execution via yaml.load() on untrusted input.", "code_usage": "The app imports yaml and parses some files, but which loader is used and whether inputs are trusted is not clear from the snippet.", "gold_label": "uncertain", "note": "Insufficient evidence to decide."}
{"id": "requests-proxy-crosshost", "package": "requests", "advisory": "Proxy-Authorization header leaked to the destination on a redirect to a different host. Only relevant when a proxy with credentials is used.", "code_usage": "The app configures an authenticated HTTP proxy and follows redirects to arbitrary external hosts.", "gold_label": "reachable", "note": "Proxy + cross-host redirects satisfy the condition."}
{"id": "requests-noproxy", "package": "requests", "advisory": "Proxy-Authorization header leaked on cross-host redirect; only relevant when a proxy is used.", "code_usage": "The app never configures a proxy; all requests are direct.", "gold_label": "not-reachable", "note": "No proxy means the leaked header never exists."}
{"id": "requests-proxy-internal", "package": "requests", "advisory": "Proxy-Authorization header leaked on cross-host redirect when a proxy is used.", "code_usage": "The app uses a proxy but only talks to internal trusted hosts; whether any response can redirect off-host is unknown.", "gold_label": "uncertain", "note": "Depends on redirect targets not shown."}
{"id": "urllib3-post-redirect", "package": "urllib3", "advisory": "Request body is resent when a redirect changes the method to GET, potentially leaking sensitive payloads.", "code_usage": "The app POSTs authentication tokens in the body and follows redirects returned by upstream services.", "gold_label": "reachable", "note": "Sensitive body + followed redirects satisfy the condition."}
{"id": "urllib3-get-only", "package": "urllib3", "advisory": "Request body is resent when a redirect changes the method to GET.", "code_usage": "The app only issues simple GET requests with no request bodies.", "gold_label": "not-reachable", "note": "No body to leak."}
{"id": "urllib3-transitive-defaults", "package": "urllib3", "advisory": "Request body resent on a method-changing redirect.", "code_usage": "urllib3 is pulled in transitively via requests; the app's redirect and body behavior is not described.", "gold_label": "uncertain", "note": "Behavior depends on unshown caller configuration."}
{"id": "jinja2-xmlattr-userkeys", "package": "jinja2", "advisory": "XSS via the xmlattr filter when attribute keys contain unsafe characters. Only relevant if xmlattr is used with attacker-influenced keys.", "code_usage": "Templates render user-provided dictionaries through the xmlattr filter.", "gold_label": "reachable", "note": "xmlattr with user-controlled keys satisfies the condition."}
{"id": "jinja2-no-xmlattr", "package": "jinja2", "advisory": "XSS via the xmlattr filter with attacker-controlled keys.", "code_usage": "The app uses Jinja2 for HTML templates but never uses the xmlattr filter anywhere.", "gold_label": "not-reachable", "note": "The vulnerable filter is never invoked."}
{"id": "jinja2-templates-unknown", "package": "jinja2", "advisory": "XSS via the xmlattr filter with attacker-controlled keys.", "code_usage": "The app uses Jinja2, but the template sources are not available to inspect.", "gold_label": "uncertain", "note": "Cannot tell whether xmlattr is used."}
{"id": "pillow-open-upload", "package": "pillow", "advisory": "Denial of service when a crafted image is opened with Image.open().", "code_usage": "The app calls Image.open() on files uploaded by end users.", "gold_label": "reachable", "note": "Attacker-controlled images reach Image.open()."}
{"id": "pillow-bundled", "package": "pillow", "advisory": "Denial of service when a crafted image is opened with Image.open().", "code_usage": "The app only opens a fixed set of static images it ships in its own package.", "gold_label": "not-reachable", "note": "No attacker-controlled image reaches Image.open()."}
{"id": "setuptools-pinned", "package": "setuptools", "advisory": "ReDoS in package_index when parsing a crafted HTML index page. Relevant only when setuptools fetches from a package index.", "code_usage": "The app pins all dependencies and never invokes setuptools' package_index at runtime.", "gold_label": "not-reachable", "note": "The vulnerable code path is not executed at runtime."}
{"id": "certifi-tls-default", "package": "certifi", "advisory": "The bundled certificate store trusted a compromised root CA, weakening TLS trust for all connections using the default bundle.", "code_usage": "The app makes outbound HTTPS requests using certifi's default certificate bundle.", "gold_label": "reachable", "note": "No narrow condition; the trust store affects all TLS verification."}
